/** Perfil de demonstração usado no painel do anunciante (seed). */
export const DEMO_PROVIDER_SLUG = "helena-jardins";

export const FALLBACK_PLATFORM_STATS = {
  profiles: 4812,
  verified: 4427,
  cities: 38,
  verifiedPct: 92,
};
