import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function ContaVerificacaoLoading() {
    return <LoadingSkeleton variant="form" ariaLabel="Carregando verificação" />;
}
