import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function CadastroClienteLoading() {
    return <LoadingSkeleton variant="form" ariaLabel="Carregando cadastro de cliente" />;
}
