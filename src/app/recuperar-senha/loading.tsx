import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function RecuperarSenhaLoading() {
    return <LoadingSkeleton variant="form" ariaLabel="Carregando recuperação de senha" />;
}
