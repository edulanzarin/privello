import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function RecuperarSenhaTokenLoading() {
    return <LoadingSkeleton variant="form" ariaLabel="Carregando recuperação" />;
}
