import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function ReelsSlugLoading() {
    return <LoadingSkeleton variant="gallery" ariaLabel="Carregando reels do perfil" />;
}
