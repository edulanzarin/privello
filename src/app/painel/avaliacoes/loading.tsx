import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function PainelAvaliacoesLoading() {
    return <LoadingSkeleton variant="list" ariaLabel="Carregando avaliações" />;
}
