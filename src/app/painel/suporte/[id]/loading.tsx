import { LoadingSkeleton } from "@/components/ui/loading-skeleton";

export default function PainelSuporteDetailLoading() {
    return <LoadingSkeleton variant="detail" ariaLabel="Carregando chamado" />;
}
