/**
 * Stub vazio — heartbeat de provider foi descontinuado junto com a feature
 * "online status". Mantido como componente null-render para evitar quebras
 * em imports históricos (`src/app/painel/layout.tsx`).
 *
 * Consumidores conhecidos:
 * - src/app/painel/layout.tsx
 */
export function ProviderHeartbeat() {
  return null;
}
